; /etc/bind/zones/db.192.168.1
; Reverse zone file for 192.168.1.x
; COPY TO: /etc/bind/zones/db.192.168.1
;
; !! ADJUST for your actual subnet !!
;
$TTL    604800
@       IN      SOA     ns1.lab.local. admin.lab.local. (
                        2026030201      ; Serial
                        3600            ; Refresh
                        1800            ; Retry
                        604800          ; Expire
                        86400 )         ; Negative Cache TTL

; Name servers
@       IN      NS      ns1.lab.local.

; PTR records — IP to name mapping (last octet only)
100     IN      PTR     ns1.lab.local.
100     IN      PTR     www.lab.local.
